{
  "name": "malakor-world-web",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "live-server --port=5173 --entry-file=index.html",
    "build": "echo \"no build step\"",
    "start": "serve -s . -l $PORT"
  },
  "devDependencies": {
    "live-server": "^1.2.2"
  },
  "dependencies": {
    "serve": "^14.2.1"
  }
}
